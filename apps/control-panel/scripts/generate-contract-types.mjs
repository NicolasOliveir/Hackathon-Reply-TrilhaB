import { readFileSync, writeFileSync } from 'node:fs'; import { resolve } from 'node:path';
const root=resolve(import.meta.dirname,'../../../packages/contracts/schemas/v1'); const common=JSON.parse(readFileSync(resolve(root,'common.schema.json'),'utf8')); const defs=common.$defs; const quote=a=>a.map(v=>`'${v}'`).join('|'); const event=JSON.parse(readFileSync(resolve(root,'event-envelope.schema.json'),'utf8')); const run=JSON.parse(readFileSync(resolve(root,'run-response.schema.json'),'utf8')); const out=`/* Generated by scripts/generate-contract-types.mjs. Do not edit manually. */\nexport type ContractVersion='${defs.contractVersion.const}'; export type RunState=${quote(defs.runState.enum)}; export type EventActor=${quote(defs.eventActor.enum)}; export type EventType=${quote(defs.eventType.enum)}; export type EventMeta={model:string|null;tokens_in:number;tokens_out:number;latency_ms:number;container_id:string|null}; export type EventEnvelope={contract_version:ContractVersion;event_id:string;sequence:number;run_id:string;ts:string;actor:EventActor;type:EventType;correlation_id:string;causation_id:string|null;task_id:string|null;payload:Record<string,unknown>;meta:EventMeta}; export type RunResponse={contract_version:ContractVersion;run_id:string;state:RunState;created_at:string;updated_at:string;current_task_id:string|null;links:{self:string;events:string}};\n`; if(!event.required.includes('meta')||!run.required.includes('contract_version')) throw new Error('Contract shape changed'); writeFileSync(resolve(import.meta.dirname,'../src/generated/contracts.ts'),out);
