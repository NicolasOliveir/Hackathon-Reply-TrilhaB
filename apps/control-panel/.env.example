# Opcional: sem esta variável, o painel usa o hostname atual na porta 8000,
# o que também funciona quando a página é aberta por um celular na rede local.
# VITE_CONTROL_API_URL=http://192.168.1.20:8000
